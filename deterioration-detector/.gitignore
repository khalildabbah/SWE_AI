# data (large / generated)
data/raw/*.csv
data/raw/*.xlsx
data/processed/*.duckdb
# user-built syndromes (Pattern Builder tab) — git has no inline comments, so
# this needs its own line or the path is taken literally and never ignored
data/custom_syndromes.json

# secrets / local config
.env

# python
.venv/
__pycache__/
*.pyc
.pytest_cache/

# node
frontend/node_modules/
frontend/dist/
