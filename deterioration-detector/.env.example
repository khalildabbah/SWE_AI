# Copy this file to .env and fill in your own values. .env is gitignored.

# MotherDuck access token (Settings -> Access Tokens in the MotherDuck UI).
# When set, the app reads the hosted cloud database instead of the local file.
MOTHERDUCK_TOKEN=

# Optional: cloud database name (defaults to "labs").
# MOTHERDUCK_DATABASE=labs

# OpenAI API key for the in-dashboard dataset chatbot (POST /api/chat).
# Get one at https://platform.openai.com/api-keys . Kept server-side only.
OPENAI_API_KEY=
# Optional: override the model (defaults to a fast, cheap model).
# OPENAI_MODEL=gpt-4o-mini
