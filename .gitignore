# Large raw data (exceeds GitHub 100MB limit)
LABEVENTS.csv
*.csv
*.duckdb
deterioration-detector/data/raw/
deterioration-detector/data/processed/

# ...but DO commit the small synthetic sample so the project runs without MIMIC
!deterioration-detector/data/sample/
!deterioration-detector/data/sample/*.csv

# ...and the small named-lab catalog that powers the Pattern Builder pick-list
!deterioration-detector/data/lab_catalog.csv

# Python
.venv/
venv/
__pycache__/
*.py[cod]
.pytest_cache/
.ipynb_checkpoints/

# OS / editor
.DS_Store
.idea/
.vscode/

# MCP / Claude config (may contain API keys/secrets)
.mcp.json
.claude/settings.local.json
